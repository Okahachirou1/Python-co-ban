# Inside the editor, complete the following steps:
# Create a tuple called fruits with the values "apple", "banana", "cherry", "orange"
# Print from the second item to third in the tuple
# Print the number of items using len()
# Unpack the tuple into three variables a, b, c, d
# Print the variable b
fruits = ("apple", "banana", "cherry", "orange")
print(fruits[1:3])
print(len(fruits))
a, b, c, d = fruits
print(b)